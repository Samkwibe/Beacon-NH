PORT=3001

# Local Docker defaults — see ../docker-compose.yml (mysql service / MYSQL_* vars)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=beacon
MYSQL_PASSWORD=beacon_local_pass
MYSQL_DATABASE=beaconnh

# Railway: reference MySQL service vars (MYSQLHOST, MYSQLUSER, …) — same names are read by this API.
# If production health shows connect ECONNREFUSED 127.0.0.1:3306, MySQL vars are missing — the app
# fell back to MYSQL_HOST defaults, not Railway’s database.
# Managed MySQL often requires TLS (set MYSQL_SSL=true if connections fail or the host requires SSL).
# On Railway, if the API logs ENOTFOUND mysql.railway.internal for MYSQLHOST, reference **MYSQL_PUBLIC_URL**
# from the MySQL service on the API service (or set MYSQL_URL to that value). The API parses JDBC URLs
# and enables TLS for *.rlwy.net hosts by default (see server/src/index.js).
# MYSQL_SSL=true
# MYSQL_SSL_REJECT_UNAUTHORIZED=false

# Optional: make GET /api/health return 503 when MySQL is down (default: always 200 so PaaS deploys succeed)
# HEALTHCHECK_REQUIRE_MYSQL=true

CORS_ORIGIN=http://localhost:5173
# Production: comma-separated SPA origins, e.g.
# CORS_ORIGIN=https://beacon-nh.vercel.app,https://www.yourdomain.org

# Firebase Admin — SAME project as VITE_FIREBASE_* on the web app.
# Put the downloaded JSON in this folder (any name). Point GOOGLE_APPLICATION_CREDENTIALS at it:
#   ./firebase-admin.json  OR  ./your-project-firebase-adminsdk-xxxx.json
# See firebase-admin.example.json for the JSON shape only (do not commit real keys).
GOOGLE_APPLICATION_CREDENTIALS=./firebase-admin.json

# Optional: AI assistant (POST /api/ai-assistant). Key stays on the server only.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
